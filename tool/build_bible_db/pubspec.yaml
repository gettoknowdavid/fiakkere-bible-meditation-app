name: build_bible_db
description: One-time build script that produces the bundled ObjectBox Bible store.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.12.2

dependencies:
  bible_models:
    path: ../../packages/bible_models
  path: ^1.9.0
  path_provider: ^2.1.6
  sqflite_common_ffi: ^2.4.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
